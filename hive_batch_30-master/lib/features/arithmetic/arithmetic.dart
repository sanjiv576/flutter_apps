class Arithmetic {
  static int add({required int first, required int second}) => first + second;
  static int sub({required int first, required int second}) => first - second;
  static int mul({required int first, required int second}) => first * second;
  static double div({required int first, required int second}) =>
      first / second;
}
