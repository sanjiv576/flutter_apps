import 'package:flutter/material.dart';

// this class only for colors of all widgets
class AppColorConstant {
  AppColorConstant._();

  static const Color primaryColor = Colors.pink;
  static const Color appBarColor = Colors.amber;
  static const Color bottomNavigationColor = Colors.green;
}
